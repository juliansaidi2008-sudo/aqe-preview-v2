# Formspree endpoint for the EV lead form. Replace with the real
# endpoint when activating Shai's account; keep `REPLACE_ME` in the
# checked-in copy so the placeholder is obvious in dev.
NEXT_PUBLIC_FORMSPREE_ENDPOINT=https://formspree.io/f/REPLACE_ME
